services:
  flask:
    restart: always
    build: .
    container_name: parser
    env_file:
      - ./.env
    ports:
      - "5000:5000"
    command: >
      gunicorn --bind 0.0.0.0:5000 'run:app'
    volumes:
      - .:/parser
